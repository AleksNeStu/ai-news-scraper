# =====================================================
# AI News Scraper — Monorepo .gitignore
# Public project — keep ALL IDE / agent state out of VCS.
#
# PUBLIC / PRIVATE boundary: see docs/PROJECT_RULES.md.
# Any agent or contributor must read that file before
# deciding what to commit. The pattern below mirrors the
# PRIVATE scope listed there.
# =====================================================

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
dist/
build/
eggs/
parts/
bin/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/
# Next.js TypeScript source — DO NOT ignore. The global `lib/` rule above is
# a Python build-output filter; it accidentally swallowed the Next.js source
# lib in apps/web/src/lib/. Whitelisted here so the app's API client, auth
# helpers, utils, and typed wrappers for AI Brief can be tracked.
!apps/web/src/lib/
!apps/web/src/lib/**/
__pypackages__/
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.hypothesis/
.coverage
.coverage.*
.cache
htmlcov/
.tox/
.nox/
nosetests.xml
coverage.xml
*.cover
*.py,cover
.prof
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt

# Virtual envs
.venv/
venv/
env/
ENV/
.virtualenv/

# ---------- Node / Next.js / pnpm ----------
node_modules/
.pnpm-store/
.pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.eslintcache
*.tsbuildinfo
next-env.d.ts
.next/
out/
.turbo/
.vercel/

# ---------- Environment & secrets ----------
.env
.env.*
!.env.example
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging

# ---------- IDE / editor ----------
.vscode/
.vscode-test/
.vscode-insiders/
.vscode-server/
.vscode-remote/
.history/
*.code-workspace
.idea/
*.swp
*.swo
*~
*.bak

# ---------- AI / agent tooling (KEEP OUT — public repo) ----------
.claude/
CLAUDE.local.md
CLAUDE.md
.roomodes
.roo/
.windsurfrules
.windsurf/
.cursor/
.aider*
.aider.chat.history.md
.aider.input.history
.continue/
.mcp.json
**/mcp.json

# ---------- TaskMaster (fully ignored — workflow runs locally) ----------
.taskmaster/

# ---------- Personal agent notes (PRD/ADRs kept local — not for public repo) ----------
.agent/

# ---------- Internal design + research notes (not for public repo) ----------
DESIGN.md
docs/research/

# ---------- Data & runtime artifacts ----------
data/
nltk_data/
chroma_db/
*.sqlite
*.sqlite3
*.db
logs/
*.log
tmp/
temp/
.cache/
.sass-cache/
desktop.ini
ehthumbs.db
Thumbs.db

# ---------- OS ----------
.DS_Store
.Spotlight-V100
.Trashes
Icon?
.directory

# ---------- Docker / build ----------
docs/_build/
site/
*.tar.gz
*.zip

# ---------- Celery (legacy, optional) ----------
celerybeat-schedule
celerybeat.pid

# TaskMaster task data (local MCP workflow state)
tasks/
